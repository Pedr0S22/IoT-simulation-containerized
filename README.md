# IoT-simulation-containerized
Implementation of a distributed system that simulates a simple IoT environment. It was used a microservices paradigm supported by docker compose orchestration, asynchronous communication through RabbitMQ and a persistent MariaDB database accessible via phpMyAdmin.
